# TransferHaiCiWordFormat

本脚本主要是用来格式化从海词词典网页版导出来的单词。
海词词典客户端不支持导出单词，只能登陆网页版，然后再导出来。把单词导出来之后，是一个txt格式的文本。然而，里面有很多不规则的空格还有换行符，实在是太难忍受了。而且，有660多个单词，一个个的去除空格，猴年马月才能弄完。后面直接写了一个脚本，将多余的空格去掉，并且添加了一个单词序列数在单词头。主要涉及到是文本的读取，修改，还有列表的操作。将一个列表置空，直接list=[]即可。
